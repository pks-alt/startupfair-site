# Approved historical photo display update

The owner approved two edited historical prize photographs in this conversation and requested that the visual changes be pushed to GitHub. This change adds display versions with the Divensi name/logo removed, without relabeling the former sponsor's check as StartupFair-issued. The two whole-image RGB checksums are copied from the owner-approved PNGs. No new artwork is generated by this integration.

About > Historical Gallery now references `public/history/homeslide2-branding-edited.png` and `public/history/september-2015-branding-edited.png`. Both original assets (`homeslide2.png` and `29.jpg`) remain preserved. The display versions are edited photographs, not unaltered historical originals. Captions, people, award amounts, dates and the existing layout are not changed by this code update. Keep the original archive for provenance.

`docs/approved-prize-photo-assets.json` records original hashes, approved pixel hashes and output hashes. The tiny lossless patch files and guarded integration script are included for reproducibility. The generated full PNG files are committed; the developer does not need to run Python to display them.

Both event telecasts already have their own local poster images, visible before clicking. Their video IDs, seven-second start for the first video, direct links and deferred player behavior remain unchanged. All four display updates are present together in this branch after verification.

Deploy the exact source commit with the complete public directory. This is a source-code/asset update only; it does not deploy to Hostinger or implement database/security work in the separate developer handoff. `main` and `recovered-baseline` are not changed.
